{
  "name": "onlyprivacy-mockup",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp -r public/. dist/ && tailwindcss -i src/input.css -o dist/styles.css --minify && esbuild src/entry.jsx --bundle --minify --outfile=dist/bundle.js --jsx=automatic"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "lucide-react": "^0.383.0",
    "firebase": "^10.13.0"
  },
  "devDependencies": {
    "esbuild": "^0.23.0",
    "tailwindcss": "^3.4.0",
    "autoprefixer": "^10.4.0",
    "postcss": "^8.4.0"
  }
}
